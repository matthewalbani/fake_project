ruby_version: "2.1.2"
bundler_version: 1.10.6
profiles:
  default:
    tests:
      - git status
      - git rev-parse HEAD
      - echo 'I am from the master'

  enumeration:
    hooks:
      enumeration: gem install octokit && ruby enumeration.rb && cat test_list.json
coverage:
  version: 2
  enabled: true
  ratchet: 80
