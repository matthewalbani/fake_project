ruby_version: "2.1.2"
environment:
  'API_KEY': 470c5a75b8a5dc5536bb7b4a75b89313b228a26
  'TEST_VAR_TEST': FromTheYmal
tests:
  - /bin/true
  - env;echo $TEST_VAR_TEST > ~/i_dont_exist.txt
test_pattern: none
profiles:
  default:
    environment:
      'cat_test': fromProfile
cache:
  key_paths:
    - REPO/Gemfile.lock		      
    - Gemfile
    - cat
  save_paths:
    - HOME/bundle 
    - HOME/cats
    - HOME/i_dont_exist.txt
  update_scripts:
    assets:      # user-provided name, can be used for logging/messaging
      key_script:  echo "15wesadasdee62"
      paths:
        - REPO/tmp/cache/assets
    assets2:      # user-provided name, can be used for logging/messaging
      key_script:  echo "1221q2131123123122"
      paths:
        - REPO/test
        # - HOME/i_dont_exist.txt
